# Include only inside the existing www HTTP/HTTPS server blocks and the
# apex HTTP server block. Preserve their TLS/listener settings. Do not include
# in the fwqgo.com HTTPS application server.
return 301 https://fwqgo.com$request_uri;
